# typeorm-sample Project Build with TypeORM

## Fork from typeorm/typeorm/sample/ repository

Steps to run this project:

1. Run `npm i` command
2. Run `npm run serve:db` command
3. Run `npm start:sample1` - `npm start:sample33` command
